<div class="search-term-box company">
    <div class="block-title-header">
        <h4 class="expandable">Blacklisted Companies</h4>
    </div>
    <div class="expandable-content">
        <hr>
        {{#blcompanies}}
            <div>{{.}}
                <button class="btn-link btn-tiny" value="{{.}}">remove?
                </button>
            </div>
        {{/blcompanies}}
    </div>
</div>