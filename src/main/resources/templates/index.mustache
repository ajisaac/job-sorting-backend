{{>header}}
<body>
<div class="root">
    <div class="main">
        <div class="main-panel panel">
            {{>header-controls}}
            {{#companies}}
                {{>company}}
            {{/companies}}
        </div>
        <div class="side-panel panel">
            {{>previous-searches}}
            {{>label-filter}}
            {{>title-filter}}
            {{>blacklisted-companies}}
        </div>
    </div>

</div>
</body>
</html>